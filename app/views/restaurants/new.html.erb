<h1>Create a new restaurant</h1>

<%= form_for @restaurant do |f| %>

  <% if @restaurant.errors.any? %>
    <div id="error_messages">
      <h2><%= pluralize(@restaurant.errors.count, "error") %> prohibited your restaurant from being saved:</h2>
      <ul>
        <% @restaurant.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :address %>
    <%= f.text_field :address %>
  </div>

  <div class="field">
    <%= f.label :capacity %>
    <%= f.number_field :capacity %>
  </div>

  <div class="button">
    <%= f.button %>
  </div>


<% end %>